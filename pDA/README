Project Structure:

bin/config.xml 				-- framework configuration, required for the PHM framework
bin/*.jar 				-- compiled and packaged code for running our DA with the PHM framework

da/DiagnosticAlgorithm.java		-- Main, mostly boiler plate code that interfaces with the PHM framework.
da/ErrorFinder.java			-- Static methods for error detection
da/Sensor.java 				-- Stores sensor information, couple of basic methods (std, mean)

util/compile.sh 			-- compiles and packs the java files into bin/
util/load.sh				-- loads the adapt-lite scenario
